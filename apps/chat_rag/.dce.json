{
  "services": {
    "dev": {
      "path": ".",
      "env_file": "./.env.local",
      "commands": {
        "UT run": {
          "commands": [
            "docker compose run tests_ut"
          ]
        },

        "App start": {
          "commands": [
            "docker compose run app_run"
          ]
        },

        "App ingests": {
          "commands": [
            "docker compose run app_ingest"
          ]
        }

      }
    }
  }
}